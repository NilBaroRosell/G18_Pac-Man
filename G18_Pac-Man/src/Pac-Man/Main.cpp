#include <exception>
#include <iostream>
#include <fstream>
#include <string>
#include "Inputs.h"

int main()
{
	bool isRunning = true;
	Inputs keyboard;
	bool* inputsKeyboard = keyboard.GetInputs();
	while (isRunning)
	{
		
	}

	system("pause");
	return 0;
}